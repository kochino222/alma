# Plan: Gamepad Bluetooth en móvil — Alma en Blanco

## Meta

Hacer que un control Bluetooth genérico de Android funcione en el juego corriendo
en navegador móvil, integrando la lectura del gamepad en el `ControlRig` existente
sin romper teclado ni táctil.

## Contexto y supuestos

- Rama actual: `feat/hud-indie` (working tree limpio). Trabajar en una rama nueva `feat/gamepad`.
- Phaser **3.80.1** (CDN). El plugin `scene.input.gamepad` (clase `GamepadPlugin`) ya existe
  y arranca solo: emite `connected` / `disconnected` y expone `pad1..pad4`, `gamepads[]`, `total`.
  El objeto por-pad es `Phaser.Input.Gamepad.Gamepad` (envuelve el nativo en `.pad`).
- Punto único de entrada de controles: `src/controles/ControlRig.js`, método `read()`
  (línea 232). Devuelve `{ left, right, up, down, axis, jump, jumpPressed, interact,
  interactPressed, attack, attackPressed, bomb, bombPressed, facing }`. `GameScene.update()`
  lo consume vía `this.controls.read()`. El `axis` es **digital** (−1/0/1): `movimiento.js`
  solo usa su signo (`Math.sign(controls.axis)`), así que el gamepad debe convertir el
  stick analógico a booleano con deadzone.
- La detección de bordes (`jumpPressed`, etc.) se calcula en `ControlRig.read()` con
  `this.previous`. Si el gamepad se fusiona **antes** de ese cálculo, los edge-triggers
  quedan correctos sin duplicar lógica (DRY).
- En Android/Chrome, `navigator.getGamepads()` devuelve vacío/nulls hasta que hay un
  **gesto del usuario** (tap/tecla) o el usuario **aprieta un botón del control**. Por eso
  hay que "cebar" la API con un gesto y además escanear por polling como respaldo.

### Sobre "pad.isAxis" (tarea 4 del pedido)

No existe `pad.isAxis` ni en la Gamepad API ni en Phaser. El problema real en móvil es otro:

1. `pad.axes` puede ser un **array vacío** (algunos pads Android solo exponen la cruceta
   como botones, o el navegador no expone ejes hasta el gesto). Leer `pad.axes[0].value`
   lanza `Cannot read properties of undefined`.
2. La **cruceta** llega a veces como botones (índices 12–15 del estándar), a veces como
   un eje "hat" discreto (−1/0/1) en índices 6/7, y a veces de ambas formas.
3. `pad.mapping` suele ser `""` (no estándar) en controles genéricos de Android: los
   índices de botones no son los de X-Input.

La solución son accesores defensivos (`axisValue`/`buttonDown`) que devuelven `0`/`false`
ante ausencia, y leer la cruceta desde **ambas** fuentes (botones y hat) con OR. Todo eso
vive en `readRawPad`, que **nunca lanza** aunque falten ejes o botones.

## Arquitectura propuesta

Nuevo módulo `src/controles/GamepadRig.js` con dos partes: (a) funciones **puras**
(`deadzone`, `readRawPad`, accesores) que no tocan `Phaser` — testables headless sin
navegador real — y (b) la clase `GamepadRig` que registra listeners `connected`/
`disconnected`, ceba la API con gestos y hace un escaneo throttled como respaldo.
`ControlRig` instancia `GamepadRig` y fusiona su salida en `read()` antes del cálculo de
bordes. No se toca `GameScene` ni `movimiento.js`.

## Tareas paso a paso

### T1 — Crear rama y verificar el gate de sintaxis

```bash
cd C:/Users/santy/Documents/GitHub/alma
git checkout -b feat/gamepad
node --check src/controles/ControlRig.js && echo "CHECK_OK"
```

Esperado: `CHECK_OK`. (Confirma que el entorno acepta `node --check` sobre módulos ES.)

### T2 — Crear el test headless (RED)

Crear `hermes/verificar-gamepad.html` (completo, copiar tal cual):

```html
<!doctype html><html><head><meta charset="utf-8"><title>PENDIENTE</title></head><body>
<pre id="v">corriendo…</pre>
<script type="module">
import { deadzone, readRawPad } from "../src/controles/GamepadRig.js";

const pasos = [];
const ok = (cond, nombre) => pasos.push({ paso: nombre, ok: !!cond });

ok(deadzone(0, 0.25) === 0, "deadzone: centro → 0");
ok(deadzone(0.1, 0.25) === 0, "deadzone: bajo umbral → 0");
ok(deadzone(0.5, 0.25) === 0.5, "deadzone: sobre umbral → valor");
ok(deadzone(-0.9, 0.25) === -0.9, "deadzone: negativo preservado");
ok(deadzone(undefined, 0.25) === 0, "deadzone: undefined → 0");

ok(readRawPad(null) === null, "readRawPad(null) → null");

// Pad sin ejes ni botones (caso "no-isAxis"): NO lanza y da todo false.
const vacio = { axes: [], buttons: [] };
let res = readRawPad(vacio);
ok(res && !res.left && !res.right && !res.up && !res.down && !res.jump, "pad sin ejes/botones → todo false");

// Stick izquierdo → direcciones (analógico → digital con deadzone).
const std = { axes: [-1, 0, 0, 0, 0, 0, 0, 0], buttons: Array.from({ length: 16 }, () => ({ pressed: false, value: 0 })) };
res = readRawPad(std);
ok(res.left === true && res.right === false, "stick X=-1 → left");
std.axes[0] = 0.9; res = readRawPad(std);
ok(res.right === true && res.left === false, "stick X=+0.9 → right");
std.axes[0] = 0; std.axes[1] = -1; res = readRawPad(std);
ok(res.up === true, "stick Y=-1 → up");
std.axes[1] = 0.6; res = readRawPad(std);
ok(res.down === true, "stick Y=+0.6 → down");
std.axes[1] = 0;

// Cruceta como botones 12-15 (estándar).
std.buttons[14].pressed = true; res = readRawPad(std);
ok(res.left === true, "dpad botón 14 → left");
std.buttons[14].pressed = false; std.buttons[15].pressed = true; res = readRawPad(std);
ok(res.right === true, "dpad botón 15 → right");
std.buttons[15].pressed = false; std.buttons[12].pressed = true; res = readRawPad(std);
ok(res.up === true, "dpad botón 12 → up");
std.buttons[12].pressed = false; std.buttons[13].pressed = true; res = readRawPad(std);
ok(res.down === true, "dpad botón 13 → down");
std.buttons[13].pressed = false;

// Cruceta como "hat" (ejes 6/7, valores -1/0/1) — Android genérico.
const hat = { axes: [0, 0, 0, 0, 0, 0, 1, 0], buttons: [] };
res = readRawPad(hat);
ok(res.right === true && res.left === false, "hat X=+1 → right");
hat.axes[6] = 0; hat.axes[7] = -1; res = readRawPad(hat);
ok(res.up === true, "hat Y=-1 → up");

// Botones por mapping estándar.
const btns = { axes: [], buttons: Array.from({ length: 16 }, () => ({ pressed: false })) };
btns.buttons[0].pressed = true; res = readRawPad(btns);
ok(res.jump === true, "botón 0 (A) → jump");
btns.buttons[0].pressed = false; btns.buttons[1].pressed = true; res = readRawPad(btns);
ok(res.interact === true, "botón 1 (B) → interact");
btns.buttons[1].pressed = false; btns.buttons[2].pressed = true; res = readRawPad(btns);
ok(res.attack === true, "botón 2 (X) → attack");
btns.buttons[2].pressed = false; btns.buttons[3].pressed = true; res = readRawPad(btns);
ok(res.bomb === true, "botón 3 (Y) → bomb");

// Mapping custom (no estándar).
const custom = { axes: [], buttons: Array.from({ length: 16 }, () => ({ pressed: false })) };
custom.buttons[9].pressed = true;
res = readRawPad(custom, { jump: 9, interact: 1, attack: 2, bomb: 3 });
ok(res.jump === true && res.interact === false, "mapping custom → jump en índice 9");

const fallos = pasos.filter(p => !p.ok);
document.title = fallos.length === 0 ? "OK" : `FALLO - ${fallos.length} pasos`;
document.getElementById("v").textContent = JSON.stringify({ total: pasos.length, fallos: fallos.length, pasos }, null, 1);
</script></body></html>
```

Correr el verifier (el módulo aún no existe → RED):

```bash
# terminal 1 (dejar corriendo en background)
python -m http.server 8123

# terminal 2
CHROME="/c/Program Files/Google/Chrome/Application/chrome.exe"
"$CHROME" --headless=new --virtual-time-budget=5000 --dump-dom \
  http://127.0.0.1:8123/hermes/verificar-gamepad.html > "$LOCALAPPDATA/Temp/gp.html" 2>&1
grep -o '<title>[^<]*</title>' "$LOCALAPPDATA/Temp/gp.html"
```

Esperado (RED): `<title>PENDIENTE</title>` (el `import` del módulo inexistente aborta el script
y el `title` queda sin sobrescribir). Si aparece `OK`, algo ya existe y hay que revisar.

### T3 — Implementar `src/controles/GamepadRig.js` (GREEN)

Crear el archivo completo:

```js
// GamepadRig - Soporte de gamepad Bluetooth en móvil - Alma en Blanco
// Lee controles genéricos de Android con mapping NO estándar y ejes faltantes.
// Las funciones puras (deadzone, readRawPad) no tocan Phaser: testables headless.

// Índices de botones según el estándar X-Input (mapping === "standard").
// Un control Android genérico puede diferir: ajustá acá o usá el volcado de debug.
export const DEFAULT_BUTTON_MAP = {
  jump: 0,      // A / Cross
  interact: 1,  // B / Circle
  attack: 2,    // X / Square
  bomb: 3       // Y / Triangle
};

export const AXIS_CFG = {
  stickX: 0,          // eje horizontal del stick izquierdo
  stickY: 1,          // eje vertical del stick izquierdo
  hatX: 6,            // cruceta digital como "hat" en algunos pads Android (-1/0/1)
  hatY: 7,
  stickDeadzone: 0.25,
  hatDeadzone: 0.5
};

export const DPAD_BUTTONS = { up: 12, down: 13, left: 14, right: 15 };

// Devuelve 0 si |v| < threshold; si no, el valor crudo. Nunca lanza.
export function deadzone(v, threshold) {
  if (typeof v !== "number" || Number.isNaN(v)) return 0;
  return Math.abs(v) < threshold ? 0 : v;
}

// Accesores defensivos: devuelven 0/false ante eje o botón ausente.
function axisValue(padLike, i) {
  const a = padLike && Array.isArray(padLike.axes) ? padLike.axes[i] : undefined;
  return typeof a === "number" ? a : (a && typeof a.value === "number" ? a.value : 0);
}

function buttonDown(padLike, i) {
  const b = padLike && Array.isArray(padLike.buttons) ? padLike.buttons[i] : undefined;
  if (!b) return false;
  if (typeof b === "boolean") return b;
  if (b.pressed !== undefined) return !!b.pressed;
  return (typeof b.value === "number" ? b.value : 0) > 0.5;
}

// Lee un pad-like ({ axes: number[], buttons: [{pressed,value}] }) y devuelve el
// fragmento de controles. NUNCA lanza aunque falten ejes o botones.
export function readRawPad(padLike, map = DEFAULT_BUTTON_MAP, cfg = AXIS_CFG) {
  if (!padLike) return null;

  const sx = deadzone(axisValue(padLike, cfg.stickX), cfg.stickDeadzone);
  const sy = deadzone(axisValue(padLike, cfg.stickY), cfg.stickDeadzone);
  const hx = deadzone(axisValue(padLike, cfg.hatX), cfg.hatDeadzone);
  const hy = deadzone(axisValue(padLike, cfg.hatY), cfg.hatDeadzone);

  // Cruceta como botones (estándar) y como hat (Android genérico), combinadas con OR.
  const dpadLeft = buttonDown(padLike, DPAD_BUTTONS.left) || hx < 0;
  const dpadRight = buttonDown(padLike, DPAD_BUTTONS.right) || hx > 0;
  const dpadUp = buttonDown(padLike, DPAD_BUTTONS.up) || hy < 0;
  const dpadDown = buttonDown(padLike, DPAD_BUTTONS.down) || hy > 0;

  return {
    left: sx < 0 || dpadLeft,
    right: sx > 0 || dpadRight,
    up: sy < 0 || dpadUp,
    down: sy > 0 || dpadDown,
    jump: buttonDown(padLike, map.jump),
    interact: buttonDown(padLike, map.interact),
    attack: buttonDown(padLike, map.attack),
    bomb: buttonDown(padLike, map.bomb)
  };
}

// Devuelve true si el pad está conectado. Acepta Phaser Gamepad o nativo.
function isConnected(source) {
  const native = source && (source.pad || source);
  return !!(native && native.connected !== false);
}

export class GamepadRig {
  constructor(scene) {
    this.scene = scene;
    this.pad = null;           // Phaser Gamepad o nativo
    this.connected = false;
    this.map = { ...DEFAULT_BUTTON_MAP };
    this._bindings = [];
    this._lastScan = 0;
    this.install();
  }

  install() {
    const scene = this.scene;
    const gp = scene.input && scene.input.gamepad;
    this.gamepad = gp || null;

    // Cebar la Gamepad API con gestos del usuario. Chrome/Android no devuelve
    // gamepads hasta una interacción (tap/tecla) o un botón del control.
    const prime = () => {
      try {
        if (navigator.getGamepads) navigator.getGamepads();
        if (navigator.webkitGetGamepads) navigator.webkitGetGamepads();
      } catch (err) { /* sin Gamepad API */ }
    };
    ["pointerdown", "touchstart", "keydown", "mousedown"].forEach(type => {
      const handler = () => prime();
      window.addEventListener(type, handler, { passive: true });
      this._bindings.push(() => window.removeEventListener(type, handler));
    });

    if (gp) {
      const onConnected = pad => {
        this.pad = pad;
        this.connected = true;
        this.log(pad);
      };
      const onDisconnected = pad => {
        if (this.pad && this.pad.index === pad.index) {
          this.pad = null;
          this.connected = false;
        }
      };
      // Eventos asíncronos del navegador, encolados y reemitidos por Phaser.
      gp.on("connected", onConnected, this);
      gp.on("disconnected", onDisconnected, this);
      this._bindings.push(() => {
        gp.off("connected", onConnected, this);
        gp.off("disconnected", onDisconnected, this);
      });
    }

    // El pad pudo conectarse antes de esta escena, o en móvil no emitir
    // 'connected' (solo aparece tras apretar un botón del control).
    this.scan();
  }

  log(pad) {
    const native = pad && pad.pad;
    console.info("[Alma][gamepad] conectado", {
      id: pad ? pad.id : null,
      mapping: native ? (native.mapping || "(vacío / no estándar)") : null,
      axes: native && native.axes ? native.axes.length : 0,
      buttons: native && native.buttons ? native.buttons.length : 0
    });
  }

  // Normaliza Phaser Gamepad (.pad) o nativo a un pad-like plano.
  toPadLike(source) {
    if (!source) return null;
    const native = source.pad || source;
    return {
      id: native.id || "",
      mapping: native.mapping || "",
      connected: native.connected !== false,
      axes: Array.isArray(native.axes)
        ? native.axes.map(a => (typeof a === "number" ? a : (a && typeof a.value === "number" ? a.value : 0)))
        : [],
      buttons: Array.isArray(native.buttons)
        ? native.buttons.map(b => ({
            pressed: !!(b && (b.pressed !== undefined ? b.pressed : (typeof b.value === "number" ? b.value > 0.5 : false))),
            value: b && typeof b.value === "number" ? b.value : 0
          }))
        : []
    };
  }

  // Busca el primer pad conectado: Phaser primero, luego navegador directo.
  scan() {
    if (this.pad && isConnected(this.pad)) return this.pad;
    const gp = this.gamepad;
    const phaserPad = gp && (gp.pad1 || (Array.isArray(gp.getAll()) ? gp.getAll().find(p => p && isConnected(p)) : null));
    if (phaserPad) { this.pad = phaserPad; this.connected = true; return phaserPad; }
    let native = null;
    try {
      const list = (navigator.getGamepads && navigator.getGamepads())
        || (navigator.webkitGetGamepads && navigator.webkitGetGamepads()) || [];
      native = Array.prototype.find.call(list, p => p && p.connected) || null;
    } catch (err) { native = null; }
    if (native) { this.pad = native; this.connected = true; }
    return native;
  }

  // Escaneo de respaldo, throttled a 1s (no golpear getGamepads por frame).
  throttledScan() {
    const now = performance.now();
    if (!this._lastScan || now - this._lastScan > 1000) {
      this._lastScan = now;
      this.scan();
    }
    return this.pad && isConnected(this.pad) ? this.pad : null;
  }

  // Devuelve el fragmento de controles, o null si no hay pad.
  read() {
    const pad = this.pad && isConnected(this.pad) ? this.pad : this.throttledScan();
    if (!pad) return null;
    return readRawPad(this.toPadLike(pad), this.map);
  }

  destroy() {
    this._bindings.forEach(remove => remove());
    this._bindings.length = 0;
    this.pad = null;
    this.connected = false;
    this.gamepad = null;
  }
}
```

Correr el test (GREEN):

```bash
"$CHROME" --headless=new --virtual-time-budget=5000 --dump-dom \
  http://127.0.0.1:8123/hermes/verificar-gamepad.html > "$LOCALAPPDATA/Temp/gp.html" 2>&1
grep -o '<title>[^<]*</title>' "$LOCALAPPDATA/Temp/gp.html"
```

Esperado (GREEN): `<title>OK</title>` (26 pasos, 0 fallos). Si falla algún paso, abrir el
`<pre id="v">` en `"$LOCALAPPDATA/Temp/gp.html"` y ver qué `ok:false`.

Gate de sintaxis:

```bash
node --check src/controles/GamepadRig.js && echo "CHECK_OK"
```

### T4 — Integrar en `ControlRig.js`

**4a.** Agregar el import (tras la línea 6, `import { VIEW_W, VIEW_H } ...`):

```js
import { GamepadRig } from "./GamepadRig.js";
```

**4b.** En el constructor, antes del cierre (tras la línea 39
`scene.events.once(Phaser.Scenes.Events.SHUTDOWN, this.destroy, this);`), agregar:

```js
    this.gamepadRig = new GamepadRig(scene);
```

**4c.** Reemplazar el método `read()` (líneas 232–270) por esta versión, que fusiona el
gamepad antes del cálculo de bordes:

```js
  read() {
    const k = this.keys;
    const gp = this.gamepadRig.read() || {};
    const leftKey = k.left.isDown || k.a.isDown;
    const rightKey = k.right.isDown || k.d.isDown;
    const upKey = k.up.isDown || k.w.isDown;
    const downKey = k.down.isDown || k.s.isDown;
    const jump = k.jump.isDown || this.virtual.jump || gp.jump;
    const interact = k.interact.isDown || this.virtual.interact || gp.interact;
    const attack = k.attack.isDown || this.virtual.attack || gp.attack;
    const bomb = k.bomb.isDown || this.virtual.bomb || gp.bomb;
    const left = leftKey || this.virtual.left || gp.left;
    const right = rightKey || this.virtual.right || gp.right;
    const up = upKey || this.virtual.up || gp.up;
    const down = downKey || this.virtual.down || gp.down;
    const out = {
      left,
      right,
      up,
      down,
      axis: (right ? 1 : 0) - (left ? 1 : 0),
      jump,
      jumpPressed: jump && !this.previous.jump,
      jumpReleased: !jump && this.previous.jump,
      interact,
      interactPressed: interact && !this.previous.interact,
      interactReleased: !interact && this.previous.interact,
      attack,
      attackPressed: attack && !this.previous.attack,
      bomb,
      bombPressed: bomb && !this.previous.bomb
    };
    this.previous.jump = jump;
    this.previous.interact = interact;
    this.previous.attack = attack;
    this.previous.bomb = bomb;
    if (out.axis !== 0) this.facing = Math.sign(out.axis);
    out.facing = this.facing;
    return out;
  }
```

**4d.** En `destroy()` (línea 272), agregar como segunda línea:

```js
    this.gamepadRig?.destroy();
```

Gate de sintaxis:

```bash
node --check src/controles/ControlRig.js && echo "CHECK_OK"
```

### T5 — Verificar regresión del juego completo (headless)

```bash
"$CHROME" --headless=new --virtual-time-budget=20000 --dump-dom \
  http://127.0.0.1:8123/hermes/verificar.html > "$LOCALAPPDATA/Temp/reg.html" 2>&1
grep -o '<title>[^<]*</title>' "$LOCALAPPDATA/Temp/reg.html"
```

Esperado: `<title>OK</title>`. Luego también:

```bash
"$CHROME" --headless=new --virtual-time-budget=20000 --dump-dom \
  http://127.0.0.1:8123/hermes/verificar-gamescene.html > "$LOCALAPPDATA/Temp/reg2.html" 2>&1
grep -o '<title>[^<]*</title>' "$LOCALAPPDATA/Temp/reg2.html"
```

Esperado: `<title>OK</title>`. **Pitfall conocido**: `verificar-gamescene.html` tiene flake
pre-existente en "recoger moneda" (timing físico, ~1 de cada 3–6 corridas). Reintentar una vez
antes de tratarlo como regresión. Ningún cambio de esta rama toca física.

### T6 — Commit del módulo + integración

```bash
git add src/controles/GamepadRig.js src/controles/ControlRig.js hermes/verificar-gamepad.html
git commit -m "feat(gamepad): soporte de gamepad Bluetooth en móvil (mapping no estándar y ejes faltantes)"
```

### T7 — Prueba manual en el celular (requiere el control físico)

1. Mergear la rama a `main` (Pages solo publica `main`) y esperar ~1 min:
   `https://kochino222.github.io/alma/` (o probar en local vía IP de la LAN con
   `python -m http.server` y el teléfono en la misma red).
2. En el teléfono: vincular el control por Bluetooth, abrir la página, **tocar la pantalla**
   una vez (gesto de desbloqueo), y luego **apretar un botón del control** (esto dispara
   `gamepadconnected` en Android).
3. Verificar: stick/cruceta mueven, A salta, X ataca, B interactúa, Y bomba.
4. Abrir la consola (vía `chrome://inspect` con USB debugging, o `vconsole`) y confirmar el
   log `[Alma][gamepad] conectado` con `id`, `mapping` y cantidad de ejes/botones.

Si los botones no coinciden, ver la sección "Descubrir el mapping real" abajo.

### T8 — (Opcional) Volcado de debug para descubrir el mapping real

Si el control genérico no responde con el mapping por defecto, pegar esto en la consola del
navegador (devtools conectado al teléfono) mientras se aprieta cada botón:

```js
window.__dumpPad = () => {
  const p = (navigator.getGamepads && navigator.getGamepads() || [])[0];
  if (!p) return "sin pad";
  console.table(p.buttons.map((b, i) => ({ index: i, pressed: b.pressed, value: b.value })));
  console.table(p.axes.map((v, i) => ({ index: i, value: v })));
  return { id: p.id, mapping: p.mapping };
};
```

Anotar el índice de cada botón (jump, interact, attack, bomb) y volcar los índices reales en
`DEFAULT_BUTTON_MAP` de `src/controles/GamepadRig.js`. Si el control expone la cruceta en
ejes distintos de 6/7, ajustar `AXIS_CFG.hatX`/`hatY`.

### T9 — PR (GitHub API; no hay `gh` CLI en esta máquina)

Seguir `references/github-api-pr.md` (del skill `alma-en-blanco-dev`): extraer el token con
`git credential fill`, crear el PR `feat/gamepad → main` vía REST. NUNCA imprimir el token.

## Tests / validación (ciclo TDD)

- **Pure logic** (sin Phaser, sin navegador): `hermes/verificar-gamepad.html` cubre deadzone,
  pad vacío ("no-isAxis"), stick → direcciones, cruceta como botones y como hat, botones por
  mapping estándar y custom. Determinista, sin flake.
- **Regresión**: `verificar.html` y `verificar-gamescene.html` (este último con el flake de
  moneda conocido).
- **Sintaxis**: `node --check` sobre los dos archivos `.js` tocados.
- **Manual**: T7 con el control físico, único modo de validar el gesto de desbloqueo real.

Comando de verificación canónico (desde la raíz del repo, con `python -m http.server 8123`
corriendo):

```bash
CHROME="/c/Program Files/Google/Chrome/Application/chrome.exe"
"$CHROME" --headless=new --virtual-time-budget=5000 --dump-dom \
  http://127.0.0.1:8123/hermes/verificar-gamepad.html > "$LOCALAPPDATA/Temp/gp.html" 2>&1
grep -o '<title>[^<]*</title>' "$LOCALAPPDATA/Temp/gp.html"   # → <title>OK</title>
```

## Riesgos, tradeoffs y preguntas abiertas

- **SSL/contexto seguro**: Chrome bloquea la Gamepad API fuera de contexto seguro
  (https o localhost). GitHub Pages y `http://127.0.0.1` son seguros; una IP LAN directa
  (`http://192.168.x.x`) **no** lo es y el gamepad no aparecerá. Para prueba LAN local,
  usar el túnel de Pages o un reverse proxy https.
- **iOS Safari**: usa `webkitGetGamepads` y soporte parcial (iOS ≥14.5). El módulo lo
  contempla, pero no está validado; el pedido apunta a Android.
- **`game.device.input.gamepads === false`**: si Phaser desactiva el plugin por detección de
  dispositivo, `gp` es `null` y el módulo cae al escaneo directo `navigator.getGamepads()`
  (cubierto por el fallback de `scan()`). Por eso no dependemos del plugin para leer.
- **Overhead de escaneo**: el fallback hace `getGamepads()` como mucho 1 vez/segundo
  (throttle), no por frame. Aceptable.
- **Mapping por defecto**: es el estándar X-Input (A/B/X/Y). Un control genérico puede no
  seguirlo; queda resuelto con `DEFAULT_BUTTON_MAP` editable + volcado de debug (T8). No se
  implementa auto-calibración "apretá cada botón" para mantener YAGNI.
- **Pregunta abierta**: ¿hace falta feedback visual de "control conectado" en el HUD? Hoy solo
  hay `console.info`. Se puede sumar un `scene.showMessage(...)` en `onConnected` si Santiago
  lo pide, pero se deja fuera para no acoplar `GamepadRig` a `GameScene`.

## Siguiente paso

Ejecutar T1→T6 en orden. Si Santiago lo aprueba, lo hago por subagente con estos pasos.
